export const ErrorList: any = {
    required: "Field is required",
    email: "Invalid email address",
    maxlength: "Fields must have at least {{requiredLength}} characters",
    minlength: "Fields must have no more than {{requiredLength}} characters",
    min: "Длина не должна быть меньше {{min}}",
    max: "Length must not exceed {{max}}",
    pattern: "Fields must contain these characters {{requiredPattern}}",
}
